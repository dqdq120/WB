## 🏗️ Web Builder Architecture

### System Architecture Diagram

```
┌────────────────────────────────────────────────────────────────────────┐
│                          WEB BUILDER APPLICATION                        │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │                         PRESENTATION LAYER                      │ │
│  ├──────────────────────────────────────────────────────────────────┤ │
│  │                                                                 │ │
│  │  ┌─────────────────┐  ┌──────────────────┐  ┌────────────────┐ │ │
│  │  │  PropertyPanel  │  │  AttributePanel  │  │   Canvas UI    │ │ │
│  │  │   (CSS Editor)  │  │   (Attr Editor)  │  │  (Grid BG)     │ │ │
│  │  └─────────────────┘  └──────────────────┘  └────────────────┘ │ │
│  │                                                                 │ │
│  │  ┌─────────────────┐  ┌──────────────────┐                    │ │
│  │  │  Add Elements   │  │  Elements Tree   │                    │ │
│  │  │  (Drag Source)  │  │  (Selection)     │                    │ │
│  │  └─────────────────┘  └──────────────────┘                    │ │
│  │                                                                 │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │                      BUSINESS LOGIC LAYER                        │ │
│  ├──────────────────────────────────────────────────────────────────┤ │
│  │                                                                  │ │
│  │                  ┌──────────────────────────┐                  │ │
│  │                  │   ElementManager         │                  │ │
│  │                  ├──────────────────────────┤                  │ │
│  │                  │ • Create Element         │                  │ │
│  │                  │ • Select/Deselect        │                  │ │
│  │                  │ • Handle Drag Drop       │                  │ │
│  │                  │ • Delete Element         │                  │ │
│  │                  │ • Export HTML            │                  │ │
│  │                  └──────────────────────────┘                  │ │
│  │                                                                  │ │
│  │  ┌──────────────────────────────────────────────────────────┐  │ │
│  │  │                   Element Collection                     │  │ │
│  │  ├──────────────────────────────────────────────────────────┤  │ │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐ ┌──────────┐ │  │ │
│  │  │  │ Element  │  │ Element  │  │ Element  │ │ Element  │ │  │ │
│  │  │  │  (Div)   │  │   (P)    │  │  (Btn)   │ │  (Input) │ │  │ │
│  │  │  └──────────┘  └──────────┘  └──────────┘ └──────────┘ │  │ │
│  │  │                                                          │  │ │
│  │  └──────────────────────────────────────────────────────────┘  │ │
│  │                                                                  │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │                         DATA LAYER                               │ │
│  ├──────────────────────────────────────────────────────────────────┤ │
│  │                                                                  │ │
│  │  ┌──────────────────┐        ┌──────────────────┐             │ │
│  │  │   Properties     │        │    Attributes    │             │ │
│  │  │  (CSS Styles)    │        │  (HTML Attrs)    │             │ │
│  │  │                  │        │                  │             │ │
│  │  │ • width/height   │        │ • id             │             │ │
│  │  │ • colors         │        │ • class          │             │ │
│  │  │ • padding/margin │        │ • title          │             │ │
│  │  │ • border/radius  │        │ • textContent    │             │ │
│  │  │ • font styling   │        │                  │             │ │
│  │  │ • position/z-idx │        │                  │             │ │
│  │  │ • opacity        │        │                  │             │ │
│  │  │ • display        │        │                  │             │ │
│  │  └──────────────────┘        └──────────────────┘             │ │
│  │                                                                  │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │                      EVENT SYSTEM                                │ │
│  ├──────────────────────────────────────────────────────────────────┤ │
│  │                                                                  │ │
│  │  Window Events:                Element Events:                  │ │
│  │  • elementSelected            • propertyChanged                │ │
│  │  • elementDeleted             • attributeChanged               │ │
│  │                               • selected/deselected             │ │
│  │                                                                  │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### Class Relationships

```
┌─────────────────────────────────────┐
│         ElementManager              │
│  (Canvas Operations & Lifecycle)    │
└────────────┬────────────────────────┘
             │
             │ manages
             │
             ↓
┌─────────────────────────────────────┐
│  Element[]                          │
│  (DOM Element Wrappers)             │
└────────────────────────────────────┘
    ↑                               ↑
    │ updates                       │ updates
    │                               │
┌────────────────┐      ┌──────────────────┐
│ PropertyPanel  │      │  AttributePanel  │
│                │      │                  │
│ • Renders      │      │ • Renders        │
│   properties   │      │   attributes     │
│ • Updates via  │      │ • Updates via    │
│   element.js   │      │   element.js     │
└────────────────┘      └──────────────────┘
```

### Data Flow Diagram

```
USER INTERACTION
      │
      ├─ Drag element from Add Panel
      │     │
      │     ↓
      │  ElementManager.createElement()
      │     │
      │     ↓
      │  Element.createDOMElement()
      │     │
      │     ↓
      │  Canvas displays element
      │
      ├─ Click element in canvas
      │     │
      │     ↓
      │  Element.select()
      │     │
      │     ↓
      │  Emit 'elementSelected' event
      │     │
      │     ↓
      │  PropertyPanel.setElement()
      │  AttributePanel.setElement()
      │
      ├─ Edit property input
      │     │
      │     ↓
      │  PropertyPanel.onPropertyChange()
      │     │
      │     ↓
      │  element.setProperty()
      │     │
      │     ↓
      │  DOM element style updated
      │     │
      │     ↓
      │  Canvas reflects change
      │
      ├─ Edit attribute input
      │     │
      │     ↓
      │  AttributePanel.onAttributeChange()
      │     │
      │     ↓
      │  element.setAttribute()
      │     │
      │     ↓
      │  DOM element attribute updated
      │     │
      │     ↓
      │  Canvas reflects change
      │
      └─ Click delete button
            │
            ↓
         Emit 'elementDeleted' event
            │
            ↓
         ElementManager.removeElement()
            │
            ↓
         Element.destroy()
            │
            ↓
         Remove from DOM
            │
            ↓
         Update panels
```

### Component Interaction

```
┌──────────────────────────────────────────────────────┐
│                      init.js                         │
│               (Integration Point)                    │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Creates instances of:                             │
│  • ElementManager (manages canvas operations)       │
│  • PropertyPanel (manages CSS editing)              │
│  • AttributePanel (manages attribute editing)       │
│                                                      │
│  Sets up event listeners:                           │
│  • Window.elementSelected → Update panels           │
│  • Window.elementDeleted → Cleanup & refresh        │
│  • Drag events → ElementManager handlers            │
│                                                      │
│  Initializes:                                       │
│  • Add Elements panel buttons                       │
│  • Canvas drop zone                                 │
│  • Tree panel sync logic                            │
│                                                      │
└──────────────────────────────────────────────────────┘
         ↓                          ↓                  ↓
    ┌─────────┐            ┌─────────────┐       ┌────────┐
    │ element │            │ PropertyPnl  │       │ Attrib │
    │  .js    │            │   panels.js  │       │ Panel  │
    │         │            │              │       │        │
    └─────────┘            └─────────────┘       └────────┘
```

### File Dependency Graph

```
index.html
    ↓
    ├─→ themes.js (Theme switching)
    │
    ├─→ element.js (Element & ElementManager classes)
    │   - Used by: init.js
    │
    ├─→ panels.js (PropertyPanel & AttributePanel classes)
    │   - Used by: init.js
    │   - Depends on: element.js indirectly
    │
    ├─→ panelManager.js (Golden Layout integration)
    │   - Used by: init.js
    │
    └─→ init.js (Main integration & setup)
        - Imports: element.js, panels.js, panelManager.js
        - Creates: ElementManager, PropertyPanel, AttributePanel
        - Establishes: Event listeners, drag-drop handlers
```

### State Management

```
Element State:
    ├─ properties: { width, height, padding, ... }
    ├─ attributes: { id, class, title, textContent }
    ├─ isSelected: boolean
    ├─ domElement: DOM reference
    └─ listeners: callback array

ElementManager State:
    ├─ elements: Element[]
    ├─ selectedElement: Element | null
    ├─ canvas: DOM reference
    └─ listeners: callback array

PropertyPanel State:
    ├─ currentElement: Element | null
    ├─ propertyInputs: { name → DOM input }
    └─ container: DOM reference

AttributePanel State:
    ├─ currentElement: Element | null
    ├─ attributeInputs: { name → DOM input }
    └─ container: DOM reference
```

### Event Flow

```
User Action
    ↓
Panel/Canvas Handler
    ↓
Element Method Called
    ↓
Element emits change event
    ↓
Panel listens & updates UI
    ↓
DOM updated
    ↓
Visual change in canvas
```

### Scaling Considerations

**Current Implementation Supports:**
- ✓ Up to 100+ elements (tested mentally)
- ✓ Real-time property updates
- ✓ Smooth drag-and-drop
- ✓ Theme switching with all elements

**Performance Optimizations:**
- Efficient event system (no polling)
- Direct DOM manipulation (not virtual DOM)
- CSS transitions for smooth visual feedback
- Lazy property updates (only on change)

**Future Scaling:**
- Add virtual scrolling for large element lists
- Implement element grouping/nesting
- Add undo/redo history system
- Implement element templates/presets

---

**Architecture is clean, modular, and extensible.**
